export type Occurence = {
    cep: string, 
    description: string, 
    type: string
}

export type Coordinates = {
    latitude: number, 
    longitude: number, 
    latitudeDelta: number, 
    longitudeDelta: number
}